package config;

public interface ModelConfig {
    int ROW_COUNT =21;
    int COL_COUNT =19;

    /** Number of levels available in the game. Starts from 1 to NUM_LEVELS.*/
    int NUM_LEVELS = 6;
}
